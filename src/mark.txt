@blur="$v.email.$touch()" какое поле будем прослушивать
  // Поскольку подключили доп плагин Vuelidate нам доступен новый объект
  validations:{
    email:{
      // В Es 6 если ключ и значение совпадает можно опустить значение оставив только ключ  required:required
      required,
      // email:email
      email
    }
  }

<!-- {
  $v в плагине vuelidate это объект
  "email": { -- указываем инициализированное поле
    "required": false,
    "$model": "",
    "$invalid": true, // Если true есть ощибка
    "$dirty": false, //Еще не трогали инпут
    "$anyDirty": false,
    "$error": false, // Нет ни какой ошибки
    "$anyError": false,
    "$pending": false, // Для асинхронных валидаторов
    "$params": { // В params записываются параметры которые используем для раличной валидации
      "required": {
        "type": "required"
      }
    }
  },
  //Глобальные объекты которые отвечают за всю форму в компоненте
  "$model": null,
  "$invalid": true,
  "$dirty": false,
  "$anyDirty": false,
  "$error": false,
  "$anyError": false,
  "$pending": false,
  "$params": {
    "email": null
  }
} -->